[pytest]
testpaths = tests
python_files = test_*.py
markers =
    integration: requires curated local geometry or mesh assets
    optional_solver: requires an optional aerodynamic or CFD solver
    timeout(seconds): fail a test that exceeds the pytest-timeout limit
